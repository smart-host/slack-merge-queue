name: test_alert_current_wait_suite

on:
  check_run:
    types: 
      - completed

jobs:

  alert_current_pr:
    env: # Or as an environment variable
      SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}
    runs-on: ubuntu-latest
    name: Alert Current in Queue after wait
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Alert queue after wait suite
        uses: ./ # Uses an action in the root directory
        id: alert
        with:
          mode: 'ALERT'
          alert_message: 'build is complete. Time to merge after a wait suite!'
          channel: 'G01C4NFBUM7'
      # Use the output from the `alert` step
      - name: Get the output status
        run: echo "status => ${{ steps.alert.outputs.status }}"